import assert from "node:assert";
import test from "node:test";

test.describe("File Service Tests...", () => {
    test.it.todo("Need to write tests for FileService");

    test.it.skip("Expects value to be zero", () => {
        assert.strictEqual(0, 0);
    });
});
