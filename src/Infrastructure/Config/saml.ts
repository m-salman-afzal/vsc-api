export const saml = {
    SAML_CERT: process.env["SAML_CERT"] as string,
    SAML_PROTOCOL: process.env["SAML_PROTOCOL"] as string,
    SAML_PATH: process.env["SAML_PATH"] as string,
    SAML_ENTRYPOINT: process.env["SAML_ENTRYPOINT"] as string,
    SAML_ISSUER: process.env["SAML_ISSUER"] as string
};
