export const google = {
    GCP_KEY: process.env["GCP_KEY"] as string,
    GCP_LOGGING_KEY: process.env["GCP_LOGGING_KEY"] as string,
    SAPPHIRE_BUCKET: process.env["SAPPHIRE_BUCKET"] as string,
    FCH_BUCKET: process.env["FCH_BUCKET"] as string
};
